<%- include('../partials/header') %>
    <%- include('../partials/navbar', { user, userRole }) %>

        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10">





            <!-- Quick Actions -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-4xl mx-auto">
                <a href="/sysadmin/users"
                    class="glass-card aspect-square flex flex-col items-center justify-center p-8 group hover:bg-gray-50 transition-all hover:-translate-y-1 hover:shadow-2xl text-center">
                    <div
                        class="w-20 h-20 rounded-full bg-gray-50 group-hover:bg-white flex items-center justify-center mb-6 shadow-sm border border-gray-100 transition-colors">
                        <i data-lucide="users" class="w-10 h-10 text-gray-900"></i>
                    </div>
                    <h2 class="text-2xl font-black text-gray-900 mb-2">User Directory</h2>
                    <p class="text-sm font-bold text-gray-400">Manage system access and roles</p>
                </a>

                <a href="/sysadmin/products"
                    class="glass-card aspect-square flex flex-col items-center justify-center p-8 group hover:bg-gray-50 transition-all hover:-translate-y-1 hover:shadow-2xl text-center">
                    <div
                        class="w-20 h-20 rounded-full bg-gray-50 group-hover:bg-white flex items-center justify-center mb-6 shadow-sm border border-gray-100 transition-colors">
                        <i data-lucide="package" class="w-10 h-10 text-gray-900"></i>
                    </div>
                    <h2 class="text-2xl font-black text-gray-900 mb-2">Product Catalog</h2>
                    <p class="text-sm font-bold text-gray-400">Manage inventory and pricing</p>
                </a>
            </div>
        </div>

        <%- include('../partials/fab', { user, userRole }) %>
            <%- include('../partials/footer') %>