<% if (typeof user !=='undefined' && user) { %>
    <nav class="sticky top-0 z-50 bg-gray-900/80 backdrop-blur-md border-b border-gray-800">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16 items-center">
                <div class="flex items-center">
                    <a href="/" class="flex items-center gap-2 group">
                        <div class="p-2 bg-indigo-600 rounded-lg group-hover:bg-indigo-500 transition-colors">
                            <i data-lucide="droplet" class="text-white w-5 h-5"></i>
                        </div>
                        <span class="text-xl font-bold text-white tracking-tight">Spenca</span>
                    </a>
                </div>

                <div class="flex items-center gap-6">
                    <div class="hidden sm:flex items-center gap-2 text-gray-300">
                        <i data-lucide="user" class="w-5 h-5"></i>
                        <span class="font-medium">
                            <<%= user.name %>>
                        </span>
                        <span
                            class="px-2 py-0.5 bg-gray-800 text-xs font-bold rounded-full text-indigo-400 uppercase tracking-widest">
                            <%= userRole %>
                        </span>
                    </div>
                    <a href="/logout"
                        class="flex items-center gap-2 px-4 py-2 text-sm font-bold text-white bg-red-600/10 hover:bg-red-600 border border-red-600/50 hover:border-red-600 rounded-xl transition-all active:scale-95">
                        <i data-lucide="log-out" class="w-4 h-4"></i>
                        <span class="hidden xs:inline">Logout</span>
                    </a>
                </div>
            </div>
        </div>
    </nav>
    <% } %>